using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BossAtack : MonoBehaviour
{/*
    [SerializeField] private int maxTime;
    [SerializeField] private int howManyDamage;
    //[SerializeField] private GameObject BossScene;
    float currTime;
    private PlayerManager PlayerManagerScript;

    void Update()
    {
        BossTimer(); 
    }

    private void BossTimer()
    {
        if (currTime >= 0)
        {
            currTime -= Time.deltaTime;
        }
        else
        {
            currTime = maxTime;
            TakeDamage();
        }
    }

    private void TakeDamage()
    {
        PlayerManagerScript.health -= howManyDamage;
        PlayerManagerScript.HealthText.text = PlayerManagerScript.health.ToString() + "/" + PlayerManagerScript.MaxHealth.ToString();
        PlayerManagerScript.HealthText2.text = PlayerManagerScript.health.ToString() + "/" + PlayerManagerScript.MaxHealth.ToString();
    }*/
}
